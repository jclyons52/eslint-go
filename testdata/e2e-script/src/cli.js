#!/usr/bin/env node
"use strict";
// The shebang is the first line of every npm bin script. ESLint rewrites it into
// a line comment of the same length before parsing, which also makes the
// "use strict" below a real directive prologue.
var count = 1
console.log(count == 1);
